# 统一频道外框与提问入口

2026-09-14：推荐、关注、热榜、故事、知识、过来人及看山统一使用同一个 shotHeader 和 shot-shell。字体、搜索框、频道顺序、选中下划线、底部按钮尺寸均采用推荐/热榜样式。

状态栏从各频道内部移到 phone-content 中，只保留一个实例。频道切换、详情、阶段设置、OAuth页、加号提问页都使用同一手机容器和字体；编辑页使用原有发布工具栏，不重复添加搜索栏。横向导航保存滚动位置并确保当前选中频道可见。

所有频道底部加号统一进入过来人提问，表单轻量标注「过来人 · 向另一程的人提问」。返回回到过来人，继续原有草稿与后端发布流程。Mock、真人问答和AI资料仍使用各自的数据链路。过来人独有的方向、阶段筛选、错位问答、个人阶段入口保留。

本地浏览器逐页测量：推荐、热榜、故事、知识、过来人均为导航18.63px、搜索/导航头93.953px、底栏89.422px、手机外框848px高（414px内容宽度），各页面仅一个状态栏。已验证从故事点加号进入过来人表单、返回激活过来人，刷新动画和真实5条问答计数正常。Mock库与刷新工作流测试、JS语法和diff检查通过。未新增或发布测试问答。
