# 统一刷新反馈

双击当前频道：滚动到顶部 → 浅灰背景的小狗写字动画 → 白底蓝字的结果条 → 自动收起。动画使用自行绘制的 SVG 与 CSS，没有插入用户整张截图或下载来源不明的动画文件。是参考效果重建，并非官方原动画文件。

推荐、关注、热榜、故事、知识和过来人共用同一刷新流程。看山在当前底部看山入口双击时也显示动画，轮换已有本地话题，不宣称重新生成 AI 答案。加载动画最少显示850ms，用户选择减少动态效果时不播放动画并缩短停留；完成条停留2400ms，再用200ms收起。提示条处于内容流内，不覆盖正文；过来人放在第四行筛选之后。

Mock 提示为「探索未知的领域，12 条示例内容推荐」，计数取当前批次；过来人显示实际返回的问答数量，不虚构新增内容。过来人真实请求超时为15秒。刷新中保留原数据，请求失败显示失败/超时并保留原内容。请求票据、DOM连接状态与操作编号共同拦截旧任务：用户中途切走，结果不能覆盖新页面。结果条收起后不残留点击区域。

验证：浏览器观察 loading/success 两帧、12条Mock实际换批、过来人5条实际计数、过来人提示位于filter-controls-spacer之后、刷新热榜期间切到知识无旧结果污染。`node tests/test_refresh_workflow.cjs`验证成功、失败保留、超时与导航取消；Mock库回归、JS语法与diff检查通过。失败/超时为模拟测试，浏览器联调使用真实本地服务。
