# 关注、推荐、热榜截图复刻

2026-09-14。基于用户提供的三张手机截图，以独立 HTML、CSS 和 SVG 重建三个演示频道。未把整张截图作为页面图片。正文与数字在 public/screen-replicas.js 的 SHOT_DATA 中修改，样式在 public/screen-replicas.css。

过来人频道、真实问答、账号登录与 AI 三问不变。周边频道的互动数字为截图示例；点赞与收藏仅在本次渲染中切换状态，不写入真人回答或数据库。关注筛选、推荐隐藏、详情及返回已接入原有事件入口。

热榜四张缩略图取自 https://www.zhihu.com/hot 页面对应问题的公开图片链接，使用知乎图片 CDN；原始版权归原作者。内容用于用户指定的界面演示，不宣称实时热榜。关注配图区目前为独立 SVG/HTML 替代素材，并非原照片，头像采用自绘 SVG。原生系统字体优先 PingFang SC；不同设备字体渲染会有差异，尚未达到逐像素完全一致。

验证：JavaScript 语法检查通过；git diff --check 通过；test_server_api.py 两项测试通过（含真实问答、点赞和模拟 AI 接口）；浏览器核验三个频道渲染、推荐点赞状态、无水平溢出、返回过来人。新静态样式接口返回 200。
